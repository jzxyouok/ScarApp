<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap>

	<!-- 获取指定模块序列名的当前序列值 -->
	<select id="getSequenceVal" parameterClass="java.lang.String" resultClass="java.lang.String" remapResults= "true">
		SELECT 
				SEQUENCE_VAL
		  FROM 
		  		S9_SYS_SEQUENCE
		 WHERE 
		 		SEQUENCE_NAME = #seqName#
	</select>
	
	<!-- 修改指定模块序列名的当前序列值 -->
	<update id="updSequenceVal" parameterClass="java.lang.String">
		 UPDATE 
		 		S9_SYS_SEQUENCE
		  	SET 
		  		SEQUENCE_VAL =   SEQUENCE_VAL + 100
		WHERE 
			SEQUENCE_NAME = #seqName#
	</update>
	
	<!-- 修改指定模块序列名的当前序列值 -->
	<update id="updSequenceValLen" parameterClass="java.util.Map">
		 UPDATE 
		 		S9_SYS_SEQUENCE
		  	SET 
		  		SEQUENCE_VAL =  SEQUENCE_VAL + #len#
		WHERE 
			SEQUENCE_NAME = #seqName#
	</update>
	
</sqlMap>